spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trab_1?&serverTimezone=UTC
    username: root
    password: senha@1234
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true

